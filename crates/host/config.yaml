# WasmLedger Host Configuration

# Extensions to load at startup
extensions:
  - id: core
    path: ../../target/wasm32-wasip2/debug/wasmledger_ext_core.wasm

# Execution limits for programs
execution:
  fuel_limit: 1000000000
  memory_limit_bytes: 104857600
  timeout_seconds: 30
