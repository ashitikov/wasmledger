[package]
name = "wasmledger-utils"
version = "0.1.0"
edition = "2024"

[dependencies]
wasmledger-sql-client = { workspace = true, features = [
  "postgres",
], optional = true }

[features]
default = ["migrations"]
migrations = [
  "wasmledger-sql-client",
]
