[package]
name = "wasmledger-migrations-macro"
version = "0.1.0"
edition = "2024"

[dependencies]
proc-macro2 = { version = "1.0" }
quote = { version = "1.0" }
syn = { version = "2.0", features = ["full"] }

[lib]
proc-macro = true
